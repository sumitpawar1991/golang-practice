root = "."
tmp_dir = "tmp"

[build]
# Linux-friendly build
cmd = "go build -o tmp/api ./cmd/api"
bin = "tmp/api"
delay = 300
exclude_dir = ["tmp", "vendor"]
exclude_regex_ = ["_test.go"]

[log]
time = true